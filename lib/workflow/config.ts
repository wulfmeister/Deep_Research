export const DEFAULT_MODEL = "zai-org-glm-4.7";

// Maximum number of search iterations per researcher topic
// Each iteration can make one web_search call
// Python reference used 5 as the limit for complex queries
export const MAX_SEARCH_ITERATIONS = 5;
